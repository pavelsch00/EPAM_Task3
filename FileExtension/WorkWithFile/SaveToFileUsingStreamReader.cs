using System;
using System.Collections.Generic;
using System.Text;

namespace XmlFileExtension.WorkWithFile
{
    class SaveToFileUsingStreamReader
    {
        /*
string material = null;
string figureName = null;
string figureType = null;
string pattern = @"(Paper|Film)([A-z]*)";
Regex regex = new Regex(pattern);
*/
        /*
    foreach (Match match in regex.Matches(typeFigure.ToString()))
    {
        figureType = typeFigure.ToString();
        material = match.Groups[1].Value;
        figureName = match.Groups[2].Value;
    }

    if(material == "Paper")
        if(figureName != "Circle")
        {

        }
*/
    }
}
